/**
 * DataTable — Public API
 */

export { DataTable } from './data-table';
export type {
  DataTableProps,
  DataTableColumn,
  PageSize,
} from './types';
export { PAGE_SIZE_OPTIONS } from './types';
